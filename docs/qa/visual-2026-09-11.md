# Visual QA — September 11, 2026

Live in-app browser, 1280 × 720 CSS pixels, device pixel ratio 2. URL: `http://localhost:5173/?controlsPreview=ready&factoryServer=local`. This was local sample data, not a live two-account multiplayer session. Screenshots were inspected in the task conversation.

## Findings

| Before | After | Why |
| --- | --- | --- |
| Avatar clothes/extras pushed Save and Cancel below the panel viewport. | Fixed: only the editor body scrolls; header and footer remain visible. Rechecked clothes, extras, changed-shirt preview and Cancel. | Critical actions must remain discoverable. `client/prototypes/factory25dAvatarEditor.css:10` |
| Keyboard shot angle persisted between separate shooting views, eventually pointing beyond 90 degrees and rejecting a shot. | Fixed: fresh shooting views reset angle and power. Verified one right-arrow press reports 5 degrees, 55% power. | A new turn must not inherit invisible aiming state. `client/prototypes/factory25dVisitorBasketball.ts:177` |
| Result text appeared while the ball was still airborne; delaying it initially exposed generic free-play instructions. | Fixed: the island retains previous HORSE letters and says “shot away” during flight. It shows the result afterward for 6.5 seconds. Screenshot at 2.2 seconds confirmed “no good · they set next” during the return to the room. | Feedback must match the visible event without changing the meaning of the bar mid-shot. `client/prototypes/factory25dBasketballChallenge.ts:186` |
| DJ monitor scales a dense search/player interface onto a small physical screen. | Open: choose a readable overlay or closer camera framing. No redesign made during this pass. | Search, track text and embedded-player controls were noticeably small. `client/prototypes/factory25dDjStation.ts:59` |
| Sound panel can open when a changing island layout moves the waveform under a stationary pointer. | Open: distinguish deliberate sound interaction from incidental hover during layout changes. | Observed during a shot; the panel temporarily covers part of the court. |

## Checks completed

- Sound panel settled rendering: two labeled sliders, blue fill, percentages and separate mute icons. Master mute/restore was checked immediately before this pass.
- Character hover: thought bubble visible, duplicate name beneath character hidden.
- Character click: profile menu opens with the corresponding row highlighted.
- Avatar editor: look, clothes, extras, live color preview, reachable actions, cancellation.
- Floor travel: downward landing, upward travel sampled at 0.20×, and settled workspace/garage. No claim of exhaustive frame-by-frame coverage.
- DJ opening, settled view, loaded YouTube player and empty queue: legibility issue recorded above.
- Free-play challenge picker: centered empty message, full-width Cancel, return to yellow challenge button.
- HORSE accept/replay/your-turn handoff, distance, initial letters, missed match earning H, next set, intentionally missed set.
- After fixes: fresh keyboard angle, stable in-flight HORSE bar, visible post-flight result.

## Motion verdict

**Block full visual sign-off** pending the incidental sound-panel opening and DJ legibility decision. The inspected picker transitions and floor endpoints did not reveal another obvious jump in this pass, but that is not approval of every animation. The user's requested height expansion and button-width morph were preserved.

## Verification and remaining coverage

- 147 test files / 1,098 tests pass after these changes.
- Production build and `git diff --check` pass.
- Still needed: narrow/mobile layouts, actual touch interactions, reduced-motion browser emulation, long populated queues/opponent lists, failure states with live authentication, and a real two-account HORSE session.
- No commit, push or deployment.
