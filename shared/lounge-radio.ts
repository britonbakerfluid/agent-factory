/** Only official YouTube embeds are played; these are curated fallbacks, not generated recommendations. */
export const DJ_VIDEOS = [
  { videoId: "LMeluRz2wv4", title: "Doomsday · MF DOOM", artist: "MF DOOM", mood: "day" },
  { videoId: "G8_HqSbOw_M", title: "All Time Low · Jon Bellion", artist: "Jon Bellion", mood: "day" },
  { videoId: "elVF7oG0pQs", title: "Heard ’Em Say · Kanye West feat. Adam Levine", artist: "Kanye West", mood: "day" },
  { videoId: "WHRnvjCkTsw", title: "Electric Relaxation · A Tribe Called Quest", artist: "A Tribe Called Quest", mood: "day" },
  { videoId: "OjHX7jf-znA", title: "The Light · Common", artist: "Common", mood: "day" },
  { videoId: "LR3GQfryp9M", title: "Dang! · Mac Miller feat. Anderson .Paak", artist: "Mac Miller", mood: "day" },
  { videoId: "vvPeJLcK2Lk", title: "Am I Wrong · Anderson .Paak feat. ScHoolboy Q", artist: "Anderson .Paak", mood: "day" },
  { videoId: "bEeaS6fuUoA", title: "Lovely Day · Bill Withers", artist: "Bill Withers", mood: "day" },
  { videoId: "76KukLYelv4", title: "Eye Know · De La Soul", artist: "De La Soul", mood: "day" },
  { videoId: "M1N_wbhAfQ4", title: "It Runs Through Me · Tom Misch feat. De La Soul", artist: "Tom Misch", mood: "day" },
  { videoId: "BAnKh2gQtVs", title: "Anita · Smino", artist: "Smino", mood: "day" },
  { videoId: "vjzOJimjqsQ", title: "FATHER FIGURE · Jon Bellion", artist: "Jon Bellion", mood: "day" },
  { videoId: "oc50wHexbwg", title: "Time (You and I) · Khruangbin", artist: "Khruangbin", mood: "day" },
  { videoId: "rKlA5tRu6f0", title: "YOU’RE THE ONE · KAYTRANADA feat. Syd", artist: "KAYTRANADA", mood: "day" },
  { videoId: "BuzJ5NArvgw", title: "Them Changes · Thundercat", artist: "Thundercat", mood: "day" },
  { videoId: "dERcdvcXuE0", title: "Rapp Snitch Knishes · MF DOOM feat. Mr. Fantastik", artist: "MF DOOM", mood: "evening" },
  { videoId: "3VxuMErCd-E", title: "Blu · Jon Bellion", artist: "Jon Bellion", mood: "evening" },
  { videoId: "ZtkNfC5Oymw", title: "Everything I Am · Kanye West feat. DJ Premier", artist: "Kanye West", mood: "evening" },
  { videoId: "nppKPgdc_u0", title: "4r Da Squaw · Isaiah Rashad", artist: "Isaiah Rashad", mood: "evening" },
  { videoId: "zmY8mG4_3j4", title: "Girl · The Internet feat. KAYTRANADA", artist: "The Internet", mood: "evening" },
  { videoId: "8k3pQ4KJtyE", title: "Diddy Bop · Noname feat. Raury & Cam O’bi", artist: "Noname", mood: "evening" },
  { videoId: "TwPCaWQIJME", title: "Gone · Kanye West feat. Consequence & Cam’ron", artist: "Kanye West", mood: "evening" },
  { videoId: "TW28iWV7nxE", title: "On & On · Erykah Badu", artist: "Erykah Badu", mood: "evening" },
  { videoId: "1ut9spXrkDw", title: "They Reminisce Over You (T.R.O.Y.) · Pete Rock & C.L. Smooth", artist: "Pete Rock & C.L. Smooth", mood: "evening" },
  { videoId: "hK0FE_zG3HE", title: "Fall In Love · Slum Village", artist: "Slum Village", mood: "evening" },
  { videoId: "uMjN_yB1jxI", title: "Prototype · Outkast", artist: "Outkast", mood: "evening" },
  { videoId: "aztpMWQdnB0", title: "Woes of the World · Saba & No ID", artist: "Saba & No ID", mood: "evening" },
  { videoId: "tLicfjOj1ZY", title: "5 Dollar Pony Rides · Mac Miller", artist: "Mac Miller", mood: "evening" },
  { videoId: "4g_WHMgUM-0", title: "Enemies · Jordan Rakei", artist: "Jordan Rakei", mood: "evening" },
  { videoId: "fw64fXeWe_8", title: "So Far to Go · J Dilla feat. Common & D’Angelo", artist: "J Dilla", mood: "evening" },
  { videoId: "j2DJbV5zyoQ", title: "Arrowroot · MF DOOM", artist: "MF DOOM", mood: "night" },
  { videoId: "0vmhgotEByc", title: "Time: The Donut of the Heart · J Dilla", artist: "J Dilla", mood: "night" },
  { videoId: "pzy1ZeX8ZOY", title: "Blu (Acoustic) · Jon Bellion", artist: "Jon Bellion", mood: "night" },
  { videoId: "hQ5x8pHoIPA", title: "Feather · Nujabes feat. Cise Starr & Akin", artist: "Nujabes", mood: "night" },
  { videoId: "uzS3WG6__G4", title: "Pink + White · Frank Ocean", artist: "Frank Ocean", mood: "night" },
  { videoId: "qYcoJpqCha4", title: "Aruarian Dance · Nujabes", artist: "Nujabes", mood: "night" },
  { videoId: "78E1ZFpHOJI", title: "Slim’s Return · Madlib", artist: "Madlib", mood: "night" },
  { videoId: "yL-_PGgFVvs", title: "A Calf Born In Winter · Khruangbin", artist: "Khruangbin", mood: "night" },
  { videoId: "zWYyycn_yCU", title: "Time Moves Slow · BADBADNOTGOOD feat. Samuel T. Herring", artist: "BADBADNOTGOOD", mood: "night" },
  { videoId: "m1WkZtA6QVk", title: "Lady · D’Angelo", artist: "D’Angelo", mood: "night" },
  { videoId: "2wK27xW4OFI", title: "Reflection Eternal · Nujabes", artist: "Nujabes", mood: "night" },
  { videoId: "dWm89BJxyp4", title: "Waves · J Dilla", artist: "J Dilla", mood: "night" },
  { videoId: "WF34N4gJAKE", title: "Cirrus · Bonobo", artist: "Bonobo", mood: "night" },
  { videoId: "lmYqOyDpz8c", title: "Speaking Gently · BADBADNOTGOOD", artist: "BADBADNOTGOOD", mood: "night" },
  { videoId: "4uS4DOHNXvg", title: "WASH · Jon Bellion", artist: "Jon Bellion", mood: "night" },
] as const;
export interface RadioEntry { id: number; videoId: string; title: string; queuedBy: string; dj: boolean }
export interface RadioScratch { type: 'radio_scratch'; entryId: number; deck: number; offset: number; serverTime: number }
export interface RadioState { type: 'radio_state'; serverTime: number; revision: number; current: (RadioEntry & { startedAt: number; duration: number; durationKnown: boolean }) | null; queue: RadioEntry[] }
export type RadioRequest = { type: 'radio_queue' } & (
  { action: 'scratch'; entryId: number; deck: number; offset: number } | { action: 'add'; videoId: string } | { action: 'remove'; entryId: number; revision: number } | { action: 'reorder'; ids: number[]; revision: number } |
  { action: 'duration'; entryId: number; seconds: number } | { action: 'skip'; entryId: number });
export interface RadioResult { type: 'radio_result'; success: boolean; error?: string; silent?: boolean }
export const RADIO_QUEUE_LIMIT = 8;
export function youtubeVideoId(input: unknown): string | undefined {
  if (typeof input !== 'string' || input.length > 2048) return;
  const value = input.trim();
  if (/^[\w-]{11}$/.test(value)) return value;
  try {
    const url = new URL(value);
    if (url.protocol !== 'https:' && url.protocol !== 'http:') return;
    const host = url.hostname.toLowerCase();
    const id = host === 'youtu.be' ? url.pathname.slice(1).split('/')[0]
      : ['youtube.com', 'www.youtube.com', 'm.youtube.com', 'music.youtube.com'].includes(host)
        ? url.pathname === '/watch' ? url.searchParams.get('v') : /^\/(shorts|embed|live)\//.test(url.pathname) ? url.pathname.split('/')[2] : null : null;
    return id && /^[\w-]{11}$/.test(id) ? id : undefined;
  } catch { return; }
}
export function radioMood(now: number) {
  const hour = Number(new Intl.DateTimeFormat('en-US', { timeZone: 'America/Denver', hour: 'numeric', hourCycle: 'h23' }).format(now));
  return hour < 7 || hour >= 22 ? 'night' : hour >= 17 ? 'evening' : 'day';
}
const ok = (): RadioResult => ({ type: 'radio_result', success: true });
const fail = (error: string): RadioResult => ({ type: 'radio_result', success: false, error });
/** Shared clock and queue, also used in the isolated local preview. */
export class LoungeRadioQueue {
  private current: RadioState['current'] = null;
  private queue: RadioEntry[] = [];
  private sequence = 0;
  private revision = 0;
  private lastDjArtist = '';
  private djCursor = { day: 0, evening: 0, night: 0 };
  private start(entry: RadioEntry, now: number) { this.current = { ...entry, startedAt: now, duration: 600, durationKnown: false }; this.revision++; }
  advance(now: number) {
    if (this.current && now < this.current.startedAt + this.current.duration * 1000) return false;
    const next = this.queue.shift();
    if (next) this.start(next, now);
    else {
      const mood = radioMood(now);
      const choices = DJ_VIDEOS.filter(v => v.mood === mood);
      let index = this.djCursor[mood] % choices.length;
      if (choices[index].artist === this.lastDjArtist) index = (index + 1) % choices.length;
      const video = choices[index];
      this.djCursor[mood] = (index + 1) % choices.length;
      this.lastDjArtist = video.artist;
      this.start({ ...video, id: ++this.sequence, queuedBy: 'lounge DJ', dj: true }, now);
    }
    return true;
  }
  enqueue(videoId: unknown, username: string, now: number, title?: string): RadioResult {
    const id = youtubeVideoId(videoId);
    if (!id) return fail('Paste a YouTube video link.');
    if (this.queue.length >= RADIO_QUEUE_LIMIT) return fail('The queue is full. Wait for a song to finish.');
    const entry = { id: ++this.sequence, videoId: id, title: (title || `YouTube · ${id}`).slice(0, 160), queuedBy: username.slice(0, 80), dj: false };
    // Human requests take over from the fallback DJ immediately.
    if (!this.current || this.current.dj) this.start(entry, now);
    else { this.queue.push(entry); this.revision++; }
    return ok();
  }
  reorder(ids: unknown, revision: unknown): RadioResult {
    if (revision !== this.revision) return fail('The queue changed. Try moving that song again.');
    if (!Array.isArray(ids) || ids.length !== this.queue.length || new Set(ids).size !== ids.length || ids.some(id => !this.queue.some(entry => entry.id === id))) return fail('That queue order is no longer available.');
    this.queue = ids.map(id => this.queue.find(entry => entry.id === id)!); this.revision++; return ok();
  }
  remove(id: unknown, revision: unknown): RadioResult {
    if (revision !== this.revision) return fail('The queue changed. Try again.');
    const index = this.queue.findIndex(entry => entry.id === id);
    if (index < 0) return fail('That song has already left the queue.');
    this.queue.splice(index, 1); this.revision++; return ok();
  }
  duration(id: unknown, seconds: unknown, now: number): RadioResult {
    if (!this.current || id !== this.current.id || typeof seconds !== 'number' || !Number.isFinite(seconds) || seconds < 5 || seconds > 10800) return fail('That video duration is unavailable.');
    if (!this.current.durationKnown) {
      // A playing embed reports duration; an old/late player cannot end a newer track.
      this.current.duration = Math.max(seconds, (now - this.current.startedAt) / 1000 + 1);
      this.current.durationKnown = true; this.revision++;
    }
    return ok();
  }
  skip(id: unknown, now: number): RadioResult {
    if (!this.current || id !== this.current.id) return fail('That song has already changed.');
    this.current = null; this.advance(now); return ok();
  }
  snapshot(now: number): RadioState {
    this.advance(now);
    return { type: 'radio_state', serverTime: now, revision: this.revision, current: this.current ? { ...this.current } : null, queue: this.queue.map(entry => ({ ...entry })) };
  }
}
